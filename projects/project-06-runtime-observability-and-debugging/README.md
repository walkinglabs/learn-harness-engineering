# Project 06. Benchmark, Cleanup, and Capstone Harness

Goal:

- Build the strongest course harness, compare it against a weaker baseline on a
  fixed task set, and perform cleanup so it is ready for repeated use.

Suggested contents:

- `baseline/`
- `stronger/`
- `cleanup/`
- `artifacts/`
