# AGENTS.md

## 项目

知识库 —— 用于文档导入、索引和问答的 Electron + TypeScript + React 桌面应用。

## 命令

- `npm run dev` —— 构建并启动应用
- `npm run check` —— 对代码库进行类型检查
- `npm run test` —— 运行测试

## 规则

- 每次只处理一个功能。
- 提交前运行 `npm run check`。
