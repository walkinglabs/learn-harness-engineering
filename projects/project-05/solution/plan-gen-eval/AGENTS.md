# AGENTS.md -- Project 05 Plan-Gen-Eval Variant

Planner writes `sprint-contract.md`, generator implements, evaluator scores against `evaluator-rubric.md`.

## Rules

- Planner defines exact scope and exclusions.
- Generator follows the sprint contract.
- Evaluator checks every risk item for transcript evidence and safety compliance.
- Run `npm run check` and `npm run test`.
