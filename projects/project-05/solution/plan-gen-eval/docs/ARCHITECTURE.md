# Architecture -- Risk Analyzer Plan-Gen-Eval

The product architecture matches the shared Interview Debrief Coach app. The workflow separates planner, generator, and evaluator.

Planner scope lives in `sprint-contract.md`; generator implements only that scope; evaluator scores the result against `evaluator-rubric.md`.
