interface:
  display_name: "Harness Creator"
  short_description: "Build requirement-linked agent harnesses"
  default_prompt: "Use $harness-creator to discover this repository's project contract, derive a requirement-linked coding-agent harness, and validate its traceability."

policy:
  allow_implicit_invocation: true
